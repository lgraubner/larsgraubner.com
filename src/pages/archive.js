// @flow
import React from 'react'

import P from '../components/Paragraph'
import { Ul } from '../components/List'

export default () => (
  <div>
    <P>
      This are old technical posts which might be still worth having, but do not
      match the current blogs concept.
    </P>

    <Ul>{'// @TODO: list posts'}</Ul>
  </div>
)
